{{- if .Values.metricbeat.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-elastic-metricbeat" .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
data:
  metricbeat.yml: |
{{ tpl .Values.metricbeat.config . | indent 4 }}
{{- end }}
